{
    "name": "My Codespace",
    "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu-22.04",
    "postCreateCommand": "sudo apt-get update -y && sudo apt-get install -y gcc make valgrind gdb libx11-dev libxext-dev python3-pip && sudo python3 -m pip install --upgrade pip setuptools && sudo python3 -m pip install norminette",
    "customizations": {
        "vscode": {
            "settings": {
                "python.pythonPath": "/usr/local/bin/python3"
            }
        },
        "extensions": [
            "ms-python.python"
        ]
    }
}
